#include <iostream>
#include "dessinable.h"
#include "supportdessin.h"

using namespace std;

Systeme( vector<Oscillateur> liste_oscillateur,Integrateur* integrateur = nullptr, Supportdessin* support = nullptr)://probeleme
 liste_oscillateur(liste_oscillateur), integrateur(integrateur), support(support) {}
 ; 
 
	
